### terraform plan
- 현재의 실행계획을 확인

### terraform apply
- 소스코드에 따라 인프라환경 구축
- auto-approve 커맨드를 옵션으로 붙이면 yes를 타이핑 안해도 된다.

### terraform destroy
- 테라폼으로 관리하는 인프라를 전부 삭제
- auto-approve 커맨드를 옵션으로 붙이면 yes를 타이핑 안해도 된다.