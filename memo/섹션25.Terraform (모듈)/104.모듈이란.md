# 104. 모듈이란

섹션25는 **Terraform 모듈(Module)** 을 다룬다.  
**104번**은 **모듈의 개념·정의·호출 방법** 개요 — **105 모듈의 정의와 이용** 실습의 기반.

슬라이드: **モジュールとは** · **モジュールについて学習する内容** · **モジュールの定義** · **モジュールの呼び出し**

이전: **103 오토 스케일링 그룹 작성** · 섹션24 마지막  
다음: **105 모듈의 정의와 이용**

---

## 모듈이란 (슬라이드)

> 리소스 생성 처리를 **하나의 덩어리**로 묶어 **호출**할 수 있게 한 것

| 비유 | 설명 |
|------|------|
| **프로그래밍의 함수** | 입력(인자)을 받아 처리하고 결과(반환값)를 돌려주는 단위 |

```
작성 중 terraform                    모듈
┌─────────────────┐              ┌─────────────┐
│  main.tf 등     │  ──호출──▶   │  ⚙️ 처리    │
│  (설정 파일)    │  ◀──결과──   │  (리소스)   │
└─────────────────┘              └─────────────┘
```

- 루트 모듈(`terraform/` 루트의 `.tf` 파일들)에서 **모듈을 호출**하면, 모듈 안에 정의된 리소스가 생성된다.
- 반복되는 인프라 패턴을 **재사용**하고 **구조를 정리**하는 데 쓴다.

---

## 학습 내용 (슬라이드)

모듈을 배울 때 핵심은 **두 가지**다.

| 구분 | 일본어 | 내용 |
|------|--------|------|
| **정의 방법** | 定義方法 | 모듈 **폴더·파일**을 만들고 리소스를 작성 |
| **호출 방법** | 呼び出し方法 | 루트 `main.tf` 등에서 **`module` 블록**으로 사용 |

```
        정의 방법                    호출 방법
           ↓                           ↓
    modules/<이름>/              module "..." { source = ... }
```

---

## 모듈의 정의 (슬라이드)

> 정의는 **지금까지 학습한 리소스 작성과 동일**하다.

### 표준 파일 구성

프로그래밍 **함수**처럼 **인자 · 처리 · 반환값** 으로 파일을 나누면 이해하기 쉽다.

```
<MODULE_NAME>/
├── variables.tf   ← 인자 (입력 변수)
├── main.tf        ← 처리 (리소스 정의)
├── outputs.tf     ← 반환값 (출력 값)
└── README.md      ← 설명 (문서)
```

| 파일 | 역할 | 함수 비유 |
|------|------|-----------|
| **`variables.tf`** | 모듈에 넘기는 **입력 변수** | 함수 **인자** |
| **`main.tf`** | **리소스 생성** 로직 | 함수 **본문(처리)** |
| **`outputs.tf`** | 호출 측에 돌려줄 **출력** | 함수 **반환값** |
| **`README.md`** | 모듈 **사용법·설명** | 주석·문서 |

### 프로젝트 디렉터리 구조 (슬라이드)

모듈은 **별도 폴더**로 분리하고, 보통 **`modules/`** 아래에 둔다.

```
<ROOT>/
├── modules/
│   └── <MODULE_NAME>/     ← 모듈 정의
│       ├── variables.tf
│       ├── main.tf
│       ├── outputs.tf
│       └── README.md
└── main.tf                ← 모듈 호출
```

예: `modules/nginx_server/` — Nginx 웹서버용 모듈

---

## 모듈의 호출 (슬라이드)

**`module` 블록**으로 호출한다. `source`에 모듈 경로를 지정하고, `variables.tf`에 정의한 변수를 **인자**처럼 넘긴다.

```hcl
module "webserver" {
  source = "./modules/nginx_server"

  instance_type = "t2.micro"
}
```

| 항목 | 설명 |
|------|------|
| **`module "webserver"`** | 이 설정 안에서 쓰는 **모듈 인스턴스 이름** (로컬 이름) |
| **`source`** | 모듈 위치 — 로컬이면 `"./modules/nginx_server"` |
| **그 아래 인자** | 모듈 `variables.tf`의 변수에 대응 |

### 출력(output) 참조 (슬라이드)

모듈의 **출력**은 **점(`.`) 표기(주소 표기)** 로 참조한다.

```hcl
module "webserver" {
  source        = "./modules/nginx_server"
  instance_type = "t2.micro"
}

output "instance_id" {
  value = module.webserver.id
}
```

| 표기 | 의미 |
|------|------|
| **`module.webserver`** | 위에서 정한 모듈 인스턴스 이름 `"webserver"` |
| **`module.webserver.id`** | 해당 모듈 `outputs.tf`의 **`id`** 출력 |

```
module "webserver" { ... }
         │
         └──▶ module.webserver.id
              (모듈 이름 · 출력 이름)
```

---

## 104번 정리

| 주제 | 핵심 |
|------|------|
| **모듈** | 리소스 묶음을 **함수처럼** 호출·재사용 |
| **정의** | `modules/<이름>/` + `variables` · `main` · `outputs` · `README` |
| **호출** | `module "이름" { source = "..." }` |
| **출력 참조** | `module.<이름>.<output>` |

현재 `terraform/` 에 **`modules/` 디렉터리는 아직 없음** — 105에서 추가·이용.
